services:
  jenkins:
    build:
      context: .
      dockerfile: docker/jenkins.Dockerfile
    image: mlops-jenkins:lts
    container_name: mlops-jenkins
    user: root
    ports:
      - "8091:8080"
      - "50000:50000"
    environment:
      - HOST_WORKSPACE_BASE=/mnt/d/ws/MLOps-jenkins-workspace
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
      - ./:/workspace
      - /mnt/d/ws/MLOps-jenkins-workspace:/var/jenkins_home/workspace
    restart: unless-stopped

volumes:
  jenkins_home:
